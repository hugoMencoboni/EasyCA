/**
 * Characterizes a convertion function has a linear function: y = a * x + b
 * y : reference unit
 * x : unite characterize
 * e.g for cm (ref. unit: m) => y = 0.01 * x + b
 */
export class UnitWeight {
    a = 1;
    b = 0;
}
